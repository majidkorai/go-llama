## Contributing

PRs and issues welcome.

- **Issues:** bug reports, feature requests, questions
- **PRs:** keep changes focused in a single file (`main.go`), test with `go build`
- **Code style:** single-file project, no external dependencies, match existing patterns
- **Web UI:** inline HTML/JS in `main.go`, no bundler
